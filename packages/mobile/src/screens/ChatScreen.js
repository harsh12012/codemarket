import React from 'react';
import ChatScreenComponent from '../components/inbox/ChatScreen';

export default function ChatScreen({ route, navigation }) {
  return <ChatScreenComponent route={route} navigation={navigation} />;
}
